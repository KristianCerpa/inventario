[phases.setup]
nixPkgs = ["nodejs_22", "php83", "php83Extensions.pdo", "php83Extensions.pdo_pgsql"]

[phases.build]
cmds = [
  "npm ci --production=false",
  "npm run build",
  "php artisan key:generate --force",
  "php artisan storage:link"
]
